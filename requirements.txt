# AI Manga Studio v0.8.0
# Novel to CG AI Video - One-Click Generation

# Core backend
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
python-multipart>=0.0.9
aiohttp>=3.9.0
requests>=2.31.0
psutil>=5.9.0

# TTS: edge-tts (free, no API key, always available)
edge-tts>=6.1.0

# TTS: Voice Cloning (optional - CosyVoice 2 for zero-shot voice cloning)
# Install separately: pip install cosyvoice
# Requires: soundfile, numpy, onnxruntime
# cosyvoice>=0.2.0

# Audio processing
soundfile>=0.12.0
numpy>=1.24.0

# Image processing
Pillow>=10.0.0

# Chinese text segmentation (story parser)
jieba>=0.42.1

# Testing
pytest>=8.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
