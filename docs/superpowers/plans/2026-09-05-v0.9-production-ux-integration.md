# v0.9 Production UX Integration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Convert the remaining v0.8 half-wired production controls into persisted, testable actions without bypassing the existing orchestration/QC contracts.

**Architecture:** Keep the unified Studio UI and existing Job/Workspace services. Add a narrow workspace endpoint for shot-level director metadata, wire the director inspector to that endpoint, and make the timeline export action use an explicit export request instead of a local notice. Keep Advanced Canvas execution fail-closed until a real resumable stage-start contract exists; expose that state clearly rather than pretending execution succeeded.

**Tech Stack:** FastAPI, Pydantic, SQLite, React 18, TypeScript, Zustand, Vitest/Testing Library.

**Spec:** `docs/superpowers/specs/2026-08-21-unified-local-studio-design.md`

## Global Constraints

- Local-first execution and storage.
- Do not bypass QC/review gates.
- Preserve asset version history and active-version semantics.
- Do not silently route an H3 Unified request to an incompatible provider/runtime.
- UI actions must reflect actual backend state; no fake-success notices for production actions.

---

### Task 1: Persist director settings on the selected asset

**Files:**
- Modify: `backend/workspace/models.py`
- Modify: `backend/workspace/repository.py`
- Modify: `backend/workspace/service.py`
- Modify: `backend/workspace/routes.py`
- Modify: `frontend/src/api/workspace.ts`
- Modify: `frontend/src/studio/StoryboardDirectorWorkspace.tsx`
- Test: `tests/workspace/test_project_assets.py`
- Test: `frontend/src/studio/StoryboardDirectorWorkspace.test.tsx`

**Interfaces:**
- Produces `PUT /api/workspace/{project_id}/assets/{asset_id}/director`.
- Request contains composition, shot size, camera movement, movement strength, focal length, lighting, emotion and prompt.
- Response is the updated `ProjectAsset`.

- [ ] Write backend and frontend failing tests.
- [ ] Verify the new endpoint/UI interaction is absent on the test-only commit.
- [ ] Add the repository/service/route/API wiring.
- [ ] Wire a `保存导演参数` action and hydrate controls from saved metadata.
- [ ] Verify tests and build.

### Task 2: Make timeline export action real and fail closed

**Files:**
- Modify: `frontend/src/studio/TimelineQcWorkspace.tsx`
- Test: `frontend/src/studio/TimelineQcWorkspace.test.tsx`

**Interfaces:**
- Uses the active/recent production Job.
- If QC has failures or no resumable Job exists, export stays disabled with an explicit reason.
- If the Job is waiting at/retryable for compose/export, reuse the existing resume/retry actions rather than launching a duplicate full pipeline.
- If export is already completed, expose the generated export asset for download instead of creating another job.

- [ ] Write failing UI tests for fail-closed export and real retry/resume action.
- [ ] Verify test failure.
- [ ] Implement the smallest orchestration-safe export action.
- [ ] Verify tests/build.

### Task 3: Remove fake execution semantics from Advanced Canvas

**Files:**
- Modify: `frontend/src/studio/AdvancedCanvasWorkspace.tsx`
- Test: `frontend/src/studio/AdvancedCanvasWorkspace.test.tsx`

**Interfaces:**
- Canvas remains a professional configuration/inspection surface.
- `运行选中节点` and `从当前节点继续` must not claim success until a stage-start contract exists.
- The UI must display that execution is delegated to the main production Job and direct the user to the Project/Timeline queue.

- [ ] Write failing tests that reject fake-success messaging.
- [ ] Verify failure.
- [ ] Replace fake-success notices with explicit delegated-execution state.
- [ ] Verify tests/build.

### Task 4: Release verification

**Files:**
- Update: this plan with completion notes only if needed.

- [ ] Run/inspect GitHub Actions for the test-only RED commit.
- [ ] Implement GREEN commits.
- [ ] Inspect frontend/H3/launcher/release CI on the final branch head.
- [ ] Open a PR against `master` with exact limitations and follow-up work.
